# T-bank Problem Solution
Solving the problem of determining the contextual frequency for each group of `similar` words.

### Usage:
1. Compile: 
```bash
g++ -O3 -std=c++17 main.cpp -o solution
```
2. Run:
```bash
./solution < input.txt
```
